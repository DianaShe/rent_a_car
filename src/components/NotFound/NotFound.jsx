import { NotFoundMessage, Text } from "./NotFound.styled"

export const NotFound = () => {
  return (
    <NotFoundMessage>
      <Text>Woops, the page is not found</Text>
    </NotFoundMessage>
  )
}
